name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  PYTHON_VERSION: "3.12"
  UV_CACHE_DIR: ~/.cache/uv

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          version: "latest"

      - name: Cache uv dependencies
        uses: actions/cache@v3
        with:
          key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-
          path: ${{ env.UV_CACHE_DIR }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Cache built dependencies
        uses: actions/cache@v3
        with:
          key: build-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          path: |
            ~/.local/share/uv/
            .venv/

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: [ build ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          version: "latest"

      - name: Restore uv cache
        uses: actions/cache@v3
        with:
          key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-
          path: ${{ env.UV_CACHE_DIR }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Restore built dependencies cache
        uses: actions/cache@v3
        with:
          key: build-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          path: |
            ~/.local/share/uv/
            .venv/

      - name: Format check with Black
        run: uv run black --check --diff .

      - name: Lint with Ruff
        run: uv run ruff check .

      - name: Type check with mypy
        run: uv run mypy src/

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [ build ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          version: "latest"

      - name: Restore uv cache
        uses: actions/cache@v3
        with:
          key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-
          path: ${{ env.UV_CACHE_DIR }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Restore built dependencies cache
        uses: actions/cache@v3
        with:
          key: build-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          path: |
            ~/.local/share/uv/
            .venv/

      - name: Run tests
        run: uv run pytest tests/ -v
